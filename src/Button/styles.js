import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  buttonR: {
    borderRadius: 13,
    borderWidth: 3,
    height: 30,
    width:70,
    justifyContent: 'center',
    alignContent: 'center',
    backgroundColor: 'red'
  },
  buttonText:{
    fontSize: 20,
    textAlign: 'center'
  }
});
